#include <stdio.h>

int main()
{
    int func, horas;
    float valor,salario; 

    scanf ("%d" "%d" , &func, &horas);
    scanf("%f", &valor);

    salario = horas * valor;

    printf("NUMBER = %d\n",func);
    printf("SALARY = U$ %.2f\n", salario);
    return 0;



}